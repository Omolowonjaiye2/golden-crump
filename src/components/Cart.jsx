import { AnimatePresence, motion } from 'framer-motion';
const money=n=>`₦${n.toLocaleString('en-NG')}`;
export default function Cart({open,onClose,items,onQty,onRemove,onCheckout}){const subtotal=items.reduce((s,i)=>s+i.price*i.qty,0);return <AnimatePresence>{open&&<><motion.div className="drawer-backdrop" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={onClose}/><motion.aside className="cart-drawer" initial={{x:'100%'}} animate={{x:0}} exit={{x:'100%'}} transition={{type:'spring',stiffness:300,damping:30}}><div className="drawer-head"><div><p className="eyebrow">YOUR SELECTION</p><h2>Your Cart</h2></div><button onClick={onClose} className="close-btn">×</button></div>{items.length?<><div className="cart-items">{items.map(i=><div className="cart-item" key={i.id}><img src={i.image} alt=""/><div className="cart-item-main"><h3>{i.name}</h3><strong>{money(i.price)}</strong><div className="qty"><button onClick={()=>onQty(i.id,-1)}>−</button><span>{i.qty}</span><button onClick={()=>onQty(i.id,1)}>+</button><button className="remove" onClick={()=>onRemove(i.id)}>Remove</button></div></div></div>)}</div><div className="cart-summary"><div><span>Subtotal</span><strong>{money(subtotal)}</strong></div><button className="btn btn-primary full" onClick={onCheckout}>Checkout <span>→</span></button></div></>:<div className="cart-empty"><div>🛍️</div><h3>Your cart is waiting.</h3><p>Add a few freshly baked favourites to get started.</p></div>}</motion.aside></>}</AnimatePresence>}
