import { useState } from 'react';
import { AnimatePresence, motion } from 'framer-motion';
import { gallery } from '../data/products';
export default function Gallery(){const [active,setActive]=useState(null);return <section id="gallery" className="section gallery"><div className="container"><div className="section-head"><div><p className="eyebrow">A FEAST FOR THE EYES</p><h2>FROM OUR <em>OVEN.</em></h2></div><p>Tap any image for a closer look.</p></div><div className="gallery-grid">{gallery.map((g,i)=><motion.button key={g.src} className={`gallery-item g${i}`} whileHover={{scale:1.015}} onClick={()=>setActive(i)}><img src={g.src} alt={g.alt}/><span>View</span></motion.button>)}</div></div><AnimatePresence>{active!==null&&<motion.div className="lightbox" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={()=>setActive(null)}><button className="lightbox-close" onClick={()=>setActive(null)}>×</button><motion.img initial={{scale:.92}} animate={{scale:1}} src={gallery[active].src} alt={gallery[active].alt} onClick={e=>e.stopPropagation()}/></motion.div>}</AnimatePresence></section>}
